/**
 * ChatPanel — re-export of ChatInput for design-doc compatibility.
 * The component is implemented in ChatInput.tsx (same functionality).
 * Import either name as needed.
 */
export { default } from './ChatInput'
